# Pizza

A Python program that simulates a pizza ordering system.
Users can select pizza size, add pepperoni, add extra cheese, and the program calculates the final bill.

# Features:

-- Choose pizza size: Small(S), Medium(M), Large(L).
-- Option to add pepperoni (extra cost depending on size).
-- Option to add extra cheese.
-- Calculates and displays the final bill.
